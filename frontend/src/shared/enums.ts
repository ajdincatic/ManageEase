export enum RequestHandleType {
  APPROVE = "APPROVE",
  DECLINE = "DECLINE",
}

export enum ExportType {
  XLS = "XLS",
  PDF = "PDF",
}

export enum UserType {
  OWNER = "OWNER",
  USER = "USER",
}

export enum DayOffType {
  ALL_TYPES = "ALL_TYPES",
  VACATION = "VACATION",
  SICK_LEAVE = "SICK_LEAVE",
  PAID_LEAVE = "PAID_LEAVE",
  UNPAID_LEAVE = "UNPAID_LEAVE",
  PAID_LEAVE_FROM_ManageEase = "PAID_LEAVE_FROM_ManageEase",
}
